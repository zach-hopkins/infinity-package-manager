# Verification fixtures

These records exercise the public evidence contract and CLI evaluator. They
use synthetic identities and are not claims about real games or mods.

`supported-configuration.json` demonstrates the boundary between Supported
and Verified: every selected part and rule is supported, but the exact clean
run, sealing, and launch-smoke gates have not been recorded.
